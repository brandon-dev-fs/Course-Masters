# Docker Compose secrets
# Copy this file to .env at the repository root, then fill in the values.
# This file is read automatically by Docker Compose for variable substitution.
# The .env file is gitignored — never commit it.

# PostgreSQL password for the containerized database
POSTGRES_PASSWORD=

# Auth secret — must be at least 32 characters
BETTER_AUTH_SECRET=
